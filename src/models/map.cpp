#include "model.h"

using namespace std;

Map::Map(string bg_url, int width, int height) : 
    bg_url(bg_url), 
    width(width), 
    height(height) 
}

const int Model::getRealPosition(int x, int y) {
    int realX = 
    return ;
}

